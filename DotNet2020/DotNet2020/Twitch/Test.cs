using System;
using System.Collections.Generic;
using System.Text;

namespace DotNet2020.Twitch
{
    public static class Test
    {
        /*
         * First interview: Given a list of items with a value, and a start and end flag,
         * implement a function that calls another function once you have a full valid set
         * (aka you have each item between start and end). A valid set might look like this:
         * { val: 0, start: false, end: false }, { val: 1, start: true, end: false }, { val: 3, start: false, end: true },
         * { val: 2, start: false, end: false }, { val: 7, start: false, end: false }
         * where the valid set only includes items 1, 2, and 3. I used a priority queue for this
         */
    }
}
